// what is the run time for:

function isEven(value) {
    if (value % 2 == 0) {
        return true;
    }
    else {
        return false;
    }
}

// Answer: 0(1), the process should be essentially the same regardless of input